<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chmykel.pfaversion1">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/actuss"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ActualiteActivity" />
        <activity android:name=".ActualiteDetail" />
        <activity android:name=".ReclamationActivity" />
        <activity android:name=".AbonnementActivity" />
        <activity android:name=".AutresAbonnementsActivity" />
        <activity android:name=".MyAbonnementsActivity"></activity>
    </application>

</manifest>